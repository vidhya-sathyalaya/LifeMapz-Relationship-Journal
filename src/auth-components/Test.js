import * as React from 'react';
import { Heading } from '@aws-amplify/ui-react';

export const DefaultHeadingExample = () => {
  return <Heading className='heading-blue' level={1}>Hello world!!!</Heading>;
}
